// Implement note management functionality using AJAX
// Example: Fetch, create, update, and delete notes

// Implement logout function
function logout() {
    // Perform logout operations (clear session, redirect to login page)
}

// Update the notesContainer with the fetched notes
function displayNotes(notes) {

    // Loop through the notes and print them
    notes.forEach(function(note) {
      console.log(note);
}

// Event listener for noteForm submission
document.getElementById('noteForm').addEventListener('submit', function (event) {
    event.preventDefault();
    // Implement AJAX request to save the note
});

// Call a function to fetch and display user's notes on page load
